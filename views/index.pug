extends layout

block content
  .container
    h1.mb-3= title
    table.table.table-striped
      thead
        tr
          th ID
          th Title
          th Year
          th Rating
          th Rate
      tbody
        each f in films
          tr
            td= f.film_id
            td= f.title
            td= f.release_year || ''
            td= f.rating || ''
            td= f.rental_rate || ''
    p.text-muted
      | API:
      code /api/films?limit=10
      |  • 
      code /api/films/1
